## Features
* Fruit shop demo built using a **Microservices architecture**
* Simulates a **Stripe payment workflow (mock payment)**
* Uses **Temporal** for **payment workflow orchestration**
* **Directus** is used as the **data management system / database API**
* Users can **chat with staff in real-time directly on the website**
